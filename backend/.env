# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=budget_db

# Server Configuration
PORT=3000

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=24h



# 🚀 Next Steps (Optional)
# Untuk production deployment, pertimbangkan:

# HTTPS enforcement
# Database connection pooling (sudah ada basic)
# Input sanitization (XSS prevention)
# Unit tests untuk critical functions
# Monitoring tools (PM2, health checks)